spring.application.name=BrknHotel
spring.datasource.url=jdbc:mysql://localhost:3306/brkn_hotel
spring.datasource.username=root
spring.datasource.password=22031112@Am
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port = 8080

# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Set the maximum file size allowed for uploads
spring.servlet.multipart.max-file-size=20MB

# Set the maximum request size (includes multi-part file requests)
spring.servlet.multipart.max-request-size=20MB

aws.s3.secret.key=${AWS_ACCESS_KEY_ID}
aws.s3.access.key=${AWS_SECRET_ACCESS_KEY}

bucket-name=rasayya-hotel-images
